<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_settings"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.quizgame.SettingsActivity"
    android:background="@android:color/black">
    <TextView
        android:text="Settings"
        android:layout_width="match_parent"
        android:id="@+id/settingsTitle"
        android:textAppearance="@style/TextAppearance.AppCompat.Display1"
        android:typeface="serif"
        android:padding="8dp"
        android:layout_height="80dp"
        android:layout_marginBottom="15dp"
        android:gravity="center"
        android:textColor="@android:color/white" />

    <ToggleButton
        android:text="ToggleButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="30dp"
        android:layout_marginTop="37dp"
        android:id="@+id/soundToggleButton"
        android:layout_below="@+id/settingsTitle"
        android:layout_alignParentEnd="true"
        android:button="@drawable/togglecheck"
        android:background="@drawable/button"
        android:onClick="onToggleBtnClick"
        />

    <TextView
        android:text="Sounds"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:typeface="serif"
        android:textSize="30sp"
        android:layout_marginEnd="56dp"
        android:id="@+id/soundText"
        android:textColor="@android:color/white"
        android:layout_alignTop="@+id/soundToggleButton"
        android:layout_toStartOf="@+id/soundToggleButton" />

</RelativeLayout>
